pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                'mvn clean package'
            }
        }
        post {
            success {
                echo 'now arching...'
                archiveArtifacts artifacts '**/target/*.war'
            }
        }
    }
}