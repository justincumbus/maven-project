pipeline {
    agent any
    stages{
        stage('Build') {
            steps {
                bat 'mvn clean packagee'
            }
        post {
            success {
                echo 'now arching...'
                archiveArtifacts artifacts: '**/target/*.war'
            }
        }
    }
}
}